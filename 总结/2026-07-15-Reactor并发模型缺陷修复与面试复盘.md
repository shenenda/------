# TinyWebServer Reactor 并发模型缺陷修复与面试复盘

> 目标：用可核验的上游源码、提交历史和压测结果，说明我不是只把 TinyWebServer 跑起来，而是定位并修复了它原有 Reactor 路径中的并发设计缺陷。

## 1. 结论先行

**原始 TinyWebServer 确实具有同一个问题。**

这里的“同一个问题”准确指：**官方源码具有事件线程自旋和无同步共享标志的并发设计缺陷**，不等于“官方原版一定会复现旧文件中的 108/359 超低吞吐”。

> 2026-07-16 复现实验修正：官方原版独立构建后，Reactor 达到约 1.24 万到 1.32 万 req/s；修复前提交重新编译后结果也相近。此前 108/359 的结果无法复现，不能再作为 1000 到 3500 倍性能提升的证据。当前可重复的修复收益是 CPU 降低约 34% 到 37%、ET/ET 吞吐基本持平，以及消除数据竞争和事件线程忙等。

截至 2026-07-16，本次对比锁定的官方仓库为：

- 官方仓库：<https://github.com/qinguoyi/TinyWebServer>
- 官方当前对比提交：[`4bcf88762f85135e9bd46a1032e815e458de6f2e`](https://github.com/qinguoyi/TinyWebServer/tree/4bcf88762f85135e9bd46a1032e815e458de6f2e)
- Reactor 实现最初引入提交：[`7a16b8f97867f81080dad1faa60df85f39684ab7`](https://github.com/qinguoyi/TinyWebServer/commit/7a16b8f97867f81080dad1faa60df85f39684ab7)，提交时间为 2020-05-11
- 我的修复前基线提交：`a57a275`
- 我的修复提交：[`7ab4ec7b013ceeb595e7e990c821288526f10616`](https://github.com/shenenda/myWebserver_didida/commit/7ab4ec7b013ceeb595e7e990c821288526f10616)

官方代码从 2020 年引入 Reactor 路径后，一直到本次检查的官方 `master`，读写事件中仍然保留以下结构：

```text
主事件线程：
    投递任务到线程池
    -> while (true) 原地轮询 improv
    -> 等工作线程修改共享标志位后才继续处理 epoll 中的其他事件
```

这会导致所谓的 Reactor 主事件循环在每次投递任务后停止分发事件，并占用 CPU 忙等工作线程。它破坏了事件循环应当具备的非阻塞分发能力，同时还包含未同步共享变量造成的 C++ 数据竞争。

我对这个问题的准确表述应该是：

> 我在复现和源码审计中定位到 TinyWebServer 官方上游仍保留的 Reactor 同步缺陷，并完成了异步化和生命周期安全改造。

不要说“这是我在全网第一个发现的 bug”。本次能够证明的是：**官方指定提交中存在该实现，我独立复现、定位、修复并验证了它**，而不是能够证明此前从未有人讨论过它。

## 2. 上游源码证据

### 2.1 主事件线程投递后原地自旋

官方 [`webserver.cpp` 第 279-355 行](https://github.com/qinguoyi/TinyWebServer/blob/4bcf88762f85135e9bd46a1032e815e458de6f2e/webserver.cpp#L279-L355) 中，Reactor 的读、写路径都具有同样结构：

```cpp
m_pool->append(users + sockfd, 0);

while (true)
{
    if (1 == users[sockfd].improv)
    {
        if (1 == users[sockfd].timer_flag)
        {
            deal_timer(timer, sockfd);
            users[sockfd].timer_flag = 0;
        }
        users[sockfd].improv = 0;
        break;
    }
}
```

写事件把状态参数换成 `1`，之后仍然执行相同的 `while (true)`。

问题不只是“循环看起来不优雅”，而是此时运行这段循环的线程正是唯一负责 `epoll_wait` 和事件分发的主线程。它等待一个连接时，其他已经就绪的连接也不能被继续分发。

### 2.2 工作线程通过普通整数发布状态

官方 [`threadpool/threadpool.h` 第 110-144 行](https://github.com/qinguoyi/TinyWebServer/blob/4bcf88762f85135e9bd46a1032e815e458de6f2e/threadpool/threadpool.h#L110-L144) 中，工作线程直接写入：

```cpp
request->improv = 1;
request->timer_flag = 1;
```

而官方 [`http/http_conn.h` 第 80-115 行](https://github.com/qinguoyi/TinyWebServer/blob/4bcf88762f85135e9bd46a1032e815e458de6f2e/http/http_conn.h#L80-L115) 把 `improv`、`timer_flag` 和 `m_state` 定义为普通 `int`，没有互斥锁、原子变量或其他完成通知机制。

`improv` 和 `timer_flag` 由工作线程写、主线程同时读，构成 C++ 内存模型下的数据竞争，程序行为未定义。即使在某次编译和某台机器上“看起来能跑”，也不能据此认为同步正确。

### 2.3 完成标志发布时机还早于真正处理完成

官方 Reactor 读成功分支的顺序是：

```cpp
if (request->read_once())
{
    request->improv = 1;
    connectionRAII mysqlcon(&request->mysql, m_connPool);
    request->process();
}
```

也就是说，名义上的完成标志在 `process()` 之前就被设置。主线程可能已经退出等待，但工作线程仍在解析 HTTP、访问数据库并生成响应。

这说明 `improv` 并不是一个语义可靠的“任务完成”事件，只是一个时序脆弱的跨线程标志。

### 2.4 提交历史证明不是本项目后加的回归

对官方完整 Git 历史执行字符串追踪：

```bash
git log -S "m_pool->append(users + sockfd, 0)" -- webserver.cpp
git log -S "request->improv = 1" -- threadpool/threadpool.h
git blame -L 286,355 webserver.cpp
git blame -L 116,142 threadpool/threadpool.h
```

两条关键路径都指向官方提交 `7a16b8f9`。本项目修复前提交 `a57a275` 继承了相同的自旋和共享标志结构，因此该问题来源于上游实现，不是本项目后来添加注释、数据库配置或压力测试时引入的。

## 3. 根因链，而不是只看“CPU 高”

### 3.1 事件循环被同步等待串行化

正确的 Reactor 目标是：

```text
epoll 返回一批就绪事件
    -> 主线程快速分发任务
    -> 立即继续处理下一事件或再次 epoll_wait
    -> 工作线程异步完成 IO/业务
```

上游实际行为是：

```text
epoll 返回一批就绪事件
    -> 投递第一个任务
    -> 主线程自旋等第一个工作线程
    -> 等到共享标志改变后才分发下一个事件
```

因此，线程池虽然有多个工作线程，事件分发入口却被逐事件同步等待限制。并发结构退化成“投递一个、等一下、再投递下一个”。

### 3.2 忙等同时与真正干活的线程争抢 CPU

`while (true)` 没有阻塞、让出或退避。尤其在单核或小规格云服务器上，主线程会持续占用时间片，工作线程反而更难及时获得 CPU 去修改 `improv`。

这形成了一个反直觉链路：

```text
主线程越着急等结果
    -> 自旋消耗越多 CPU
    -> 工作线程越晚运行
    -> 主线程等待越久
    -> 整体吞吐越低
```

所以“Reactor 模式 CPU 看起来不高”不能证明它高效。也可能是服务器大部分请求没有完成、线程调度和事件分发已被卡住。必须结合吞吐、完成请求数、失败数和每线程 CPU 一起判断。

### 3.3 普通变量跨线程读写是未定义行为

这不是简单的“可能读到旧值”。在 C++ 中，两个线程无同步地读写同一普通变量且至少一方写入，就是数据竞争，程序行为未定义。编译器没有义务保证主线程每轮都重新从内存读取 `improv`。

`volatile` 也不能解决这个问题，因为它不提供：

- 原子性；
- 线程间 happens-before 关系；
- 内存可见性顺序保证；
- 复合状态的一致性。

### 3.4 任务状态不应临时写进共享连接对象

上游 `append(request, state)` 会把读写状态写入 `request->m_state`，队列里只保存 `request` 指针。这让“本次队列任务的上下文”和“可复用连接对象的长期状态”耦合在一起。

更稳妥的设计是让每个队列项独立保存：

```cpp
struct work_item
{
    http_conn *request;
    int state;
    std::uint64_t generation;
};
```

这样，读写状态属于任务，不再依赖随后可能变化的连接成员。

## 4. 为什么不能只做一个表面修补

### 4.1 只把 `int` 改成 `std::atomic<int>` 不够

原子变量可以消除相应标志位的数据竞争，却不会改变主事件线程仍在原地等待的事实。事件循环依旧不能继续分发其他连接，因此架构瓶颈仍然存在。

### 4.2 用条件变量替换自旋也不够

条件变量会降低忙等 CPU，但主事件线程仍然会阻塞等待某一个任务。它解决的是“怎么等”，没有解决“事件循环不应该等”。

### 4.3 直接让工作线程关闭连接也不稳妥

连接关闭同时涉及：

- 从 epoll 删除 fd；
- 关闭 socket；
- 更新连接计数；
- 删除主线程维护的定时器；
- 防止 fd 被系统快速复用后，旧任务误操作新连接。

如果工作线程直接操作定时器链表和连接生命周期，就会把原本由主事件线程管理的资源变成多线程共享资源，进一步引入锁、悬空指针、重复关闭和 fd ABA 风险。

正确方向不是把自旋循环简单删除，而是重新明确成功路径、失败路径和资源所有权。

## 5. 我的修复方案

### 5.1 成功路径完全异步，不再回传“完成标志”

Reactor 主线程现在只做：

```text
收到 EPOLLIN/EPOLLOUT
    -> 刷新定时器
    -> 将 work_item 投递到线程池
    -> 立即返回事件循环
```

工作线程负责执行 Reactor 的 socket IO 和业务处理。成功时，现有 `read_once()`、`process()`、`write()` 路径根据下一阶段通过 `EPOLLONESHOT` 重新注册读或写事件。

这里不需要再额外通知主线程“成功了”，因为重新武装 epoll 本身就是后续状态推进机制。

### 5.2 失败路径使用 `eventfd` 回到主事件循环

工作线程处理失败时：

1. 把 `{sockfd, generation}` 放入互斥锁保护的失败队列；
2. 写 `eventfd`；
3. `eventfd` 已注册到同一个 epoll；
4. 主事件线程收到该事件后，统一关闭连接并删除定时器。

数据通道和唤醒通道被明确分开：

```text
失败详情：mutex + queue
唤醒通知：eventfd
资源回收：主事件线程
```

选择 `eventfd` 的原因是它是 Linux 为事件通知提供的轻量级文件描述符，可以直接接入 epoll；相比让主线程阻塞在条件变量上，它不会破坏统一事件循环。

### 5.3 队列项保存不可变任务上下文

线程池队列由 `std::list<T *>` 改为保存 `work_item`：

```cpp
struct work_item
{
    T *request;
    int state;
    std::uint64_t generation;
};
```

这样每个任务拿到的是投递时确定的读写状态和连接代次，不再通过 `http_conn::m_state` 临时传递任务参数。

### 5.4 用连接代次过滤迟到的失败通知

文件描述符只是一个可复用整数。例如旧连接 fd=10 关闭后，新连接可能很快再次得到 fd=10。

如果旧工作任务稍后报告“10 号连接失败”，只按 fd 处理就可能关闭新的连接。修复后，每次接收连接都会递增 `generation`，失败通知同时携带该值；主线程只处理 fd 和 generation 都匹配的通知。

准确地说，这一机制保护的是**迟到失败通知的关闭动作**，避免旧失败误关新连接；它不是对所有可能的旧任务访问做万能保护。

### 5.5 补齐线程和定时器生命周期

为了让异步化之后的资源生命周期可控，修复还包括：

- 线程从 detached 改为 joinable；
- 析构时设置停止状态、唤醒全部线程并 `pthread_join`；
- 先等待工作线程退出，再销毁连接数组等共享资源；
- 删除定时器后把对应指针置空；
- 异常事件处理前检查定时器是否仍存在，避免重复关闭；
- 任务队列满时立即走连接清理路径；
- 监听 backlog 从固定 `5` 调整为 `SOMAXCONN`，减少高并发连接建立阶段的排队瓶颈。

其中 backlog 调整是高并发配套优化，不是 Reactor 自旋缺陷本身。面试时必须把两者区分开。

## 6. 修复效果与证据边界

### 6.1 2026-07-16 官方原版独立复现

本次不再用“本项目继承了相同代码”代替官方实测，而是在云服务器 `/tmp` 中单独克隆官方仓库：

- 官方提交：`4bcf88762f85135e9bd46a1032e815e458de6f2e`；
- 官方 `makefile` 默认 `DEBUG=1`，实际使用 `g++ ... -g -lpthread -lmysqlclient`；
- 仅替换 `main.cpp` 中数据库用户名、密码、库名三行，`git diff --numstat` 为 `3 3 main.cpp`；
- 2 核云服务器、默认 8 个线程、关闭日志；
- 统一使用 `webbench -c 100 -t 30 http://127.0.0.1:9016/`；
- 每组独立启动和停止服务器，测试前后均检查 HTTP 200；
- 服务器 CPU 使用 `/proc/<pid>/stat` 在压测区间内计算，并保留 `pidstat` 原始采样。

官方原版结果：

| 官方模式 | 完成请求/30s | 吞吐 | 服务器 CPU | 失败请求 |
|---|---:|---:|---:|---:|
| Reactor LT + LT | 397046 | 13234.87 req/s | 114.31% | 0 |
| Reactor ET + ET | 371031 | 12367.70 req/s | 116.87% | 0 |
| Proactor LT + LT | 571452 | 19048.40 req/s | 78.23% | 0 |
| Proactor ET + ET | 510682 | 17022.73 req/s | 68.77% | 0 |

这说明：

1. 官方原版确实能正常跑，不存在“本机必然只有个位数 req/s”的现象。
2. 官方 Reactor 在本次场景下仍表现出更高 CPU、更低吞吐，但 Reactor 与 Proactor 的 IO 分工不同，不能把全部差距单独归因于自旋。
3. 官方源码中的数据竞争属于正确性缺陷，即使一次压测没有卡死，也不能说明同步方式正确。

### 6.2 修复前提交重新编译，推翻旧异常基线

为了检查仓库中原来的 108/359 是否来自本项目代码，另外把修复前提交 `a57a275` 隔离检出并从源码重新编译。

| 模式 | 旧文件记录 | 重新编译后完成请求/30s | 重新编译后吞吐 | CPU | 失败请求 |
|---|---:|---:|---:|---:|---:|
| Reactor LT + LT | 359 | 394870 | 13162.33 req/s | 115.08% | 0 |
| Reactor ET + ET | 108 | 373618 | 12453.93 req/s | 116.96% | 0 |

官方原版和 `a57a275` 重编译版都无法复现旧结果，因此：

> `benchmark_results/reactor-ltlt.txt` 和 `reactor-etet.txt` 只能视为历史异常记录，不能作为修复带来 1072 倍或 3529 倍提升的证据。

目前无法从现存信息唯一还原旧异常原因。可能因素包括旧二进制没有对应当时源码、进程状态异常或测试流程不一致。没有证据时，不应挑一个原因强行下结论。

### 6.3 同条件修复前后对照

修复前后都使用本项目 6573 字节首页、相同编译命令和相同测试脚本：

| 模式 | 版本 | 完成请求/30s | 吞吐 | 服务器 CPU | 失败请求 |
|---|---|---:|---:|---:|---:|
| LT + LT | 修复前 `a57a275` | 394870 | 13162.33 req/s | 115.08% | 0 |
| LT + LT | 修复后 `31bdcee` | 369548 | 12318.27 req/s | 76.20% | 0 |
| ET + ET | 修复前 `a57a275` | 373618 | 12453.93 req/s | 116.96% | 0 |
| ET + ET | 修复后 `31bdcee` | 374153 | 12471.77 req/s | 74.20% | 0 |

修复效果：

- LT/LT：CPU 降低 33.79%，吞吐下降 6.41%，单位 CPU 吞吐提高 41.34%；
- ET/ET：CPU 降低 36.56%，吞吐提高 0.14%，单位 CPU 吞吐提高 57.85%；
- 两组均为 0 失败，压测后首页仍返回 HTTP 200。

这是一轮受控测试，6.4% 和 0.14% 的吞吐变化还需要多轮重复才能判断是否稳定。可以明确说的是：**修复移除了忙等，把 Reactor 的 CPU 成本降低约三分之一，同时在本次测试中把吞吐保持在同一数量级。**

### 6.4 面试时必须主动说明的实验边界

专业表达是：

> 我最初观察到三个数量级的异常差距，但在补做官方原版和修复前提交的隔离重编译后发现该结果无法复现，因此撤回了错误的性能归因。受控对照显示，修复的可靠收益是消除数据竞争和事件线程忙等，服务器 CPU 下降约 34% 到 37%，ET 吞吐基本持平，LT 吞吐在本轮下降约 6.4%。

这种主动纠正比坚持一个夸张数字更能体现工程能力。

测试仍有以下限制：压测端与服务端位于同一台 2 核主机；每种模式只保留一轮正式结果；Webbench 不提供 P95/P99；修复提交还包含连接回收和生命周期改动，不是只删除自旋的单变量实验。

完整原始数据和脚本保存在：

```text
benchmark_results/2026-07-16-controlled-reproduction/
```

## 7. 这个改动真正有价值的地方

### 7.1 从“会使用 epoll”进阶到“理解事件循环约束”

很多项目描述只会写“使用 epoll + 线程池实现高并发”。这次改动能够证明我理解：

- 事件线程不能等待单个任务，否则批量就绪事件会被串行化；
- 线程池存在不等于系统真的并发；
- Reactor/Proactor 的关键是 IO 和业务责任如何分配，而不是命令行参数叫什么；
- `EPOLLONESHOT` 需要与重新武装和单连接并发访问约束配合。

### 7.2 从“代码能跑”进阶到“遵守 C++ 内存模型”

修复不是把 `improv` 改成 `volatile`，而是取消错误的共享标志同步，让跨线程通知通过明确的同步原语完成。这能够体现对以下问题的理解：

- data race 与 race condition 的区别；
- happens-before；
- 原子变量能解决什么、不能解决什么；
- 为什么事件通知和共享数据仍需各自的同步策略。

### 7.3 从“修性能”进阶到“处理资源所有权”

删除自旋很容易，安全删除自旋并不容易。真正的工程工作是处理：

- 谁负责关闭 socket；
- 谁负责删除定时器；
- 失败怎么跨线程回到事件循环；
- fd 复用后如何识别迟到通知；
- 服务器退出时如何保证线程不再访问已释放资源。

这部分比单纯背诵 Reactor 定义更有面试价值。

## 8. 30 秒面试话术

> 我的项目基于 TinyWebServer。我通过源码和 Git 历史确认，官方 Reactor 路径从 2020 年引入后一直存在主事件线程投递任务后自旋、普通整型标志跨线程读写的问题。我没有只把标志改成 atomic，而是移除主线程等待，把任务状态封装成不可变队列项，成功路径通过 EPOLLONESHOT 推进，失败路径用 eventfd 回到主循环统一回收，并用 generation 防止迟到通知误关复用 fd。受控压测中，修复后 Reactor 的服务器 CPU 降低约 34% 到 37%，ET 吞吐基本持平，LT 吞吐下降约 6.4%，两组均 0 失败。更重要的是，我补跑官方原版后发现最初的千倍提升无法复现，并主动撤回了错误归因。

## 9. 两分钟 STAR 话术

### S：背景与现象

> 我最初在云服务器上得到一组异常结果：Proactor 每秒上万请求，而 Reactor 只有个位数到十余 req/s。这个差距大到不能直接相信，所以我把任务从“优化性能”改成先验证基线是否真实。

### T：我的任务

> 我的目标不是把参数调高，而是确认结果是否真实、问题来自我的改动还是官方上游，并在不破坏定时器、EPOLLONESHOT 和 Proactor 的前提下修复它。

### A：定位与方案

> 我从 `eventLoop -> dealwithread/dealwithwrite -> threadpool::run` 追踪执行链，并用 `git log -S` 和 `git blame` 确认官方上游存在自旋和普通 `int` 跨线程同步。我没有只改成 atomic，因为那仍会阻塞事件循环；我把队列项改为携带读写状态和 generation 的不可变任务，成功路径异步重置 EPOLLONESHOT，失败路径用 eventfd 唤醒主循环，由主线程统一清理 timer 和 socket，同时改成 joinable 线程保证退出顺序。之后我单独克隆官方版本，并把修复前、修复后提交全部隔离重编译，用统一脚本记录吞吐、CPU、失败数和测试后 HTTP 状态。

### R：结果与价值

> 独立复现证明，官方原版和修复前重编译版都能达到约 1.24 万到 1.32 万 req/s，最初的 108/359 无法复现，所以我撤回了千倍提升结论。可信结果是：修复后 LT/LT CPU 降低 33.79%、吞吐下降 6.41%，ET/ET CPU 降低 36.56%、吞吐基本持平，失败数均为 0。改动让事件循环恢复非阻塞分发，也明确了跨线程通知和连接生命周期的所有权。

## 10. 简历写法

推荐写成：

> 审计并修复 TinyWebServer 上游 Reactor 并发路径：定位 epoll 主线程投递后自旋、普通标志位跨线程数据竞争及连接回收时序问题；以不可变任务上下文、eventfd 失败通知、fd generation 校验和可回收线程生命周期完成异步化改造；同机 Webbench 100 并发下服务器 CPU 降低约 34% 到 37%，ET 吞吐基本持平，LT 吞吐变化为 -6.4%，两组均 0 失败。

更保守的版本：

> 基于源码审计与受控复现实验重构 TinyWebServer Reactor 路径，移除主事件线程忙等，使用 eventfd 将工作线程失败事件纳入 epoll，并补齐连接代次与线程退出管理；修复后保持约 1.2 万 req/s 吞吐，同时显著降低服务器 CPU。

不要写成：

- “重写了 Linux 内核 Reactor”；
- “解决了 epoll 官方 bug”；
- “QPS 永久提升 3500 倍”；
- “实现了生产级 Web 服务器”；
- “首次发现 TinyWebServer 漏洞”。

## 11. 高频追问与回答

### Q1：为什么自旋会让 Reactor 退化？

因为自旋发生在唯一的 epoll 分发线程中。它等待当前任务时，不能处理同一批次后续事件，也不能及时再次进入 `epoll_wait`。多工作线程无法弥补分发入口被同步阻塞的问题。

### Q2：为什么不用 `std::atomic<bool>`？

atomic 只能让标志位读写符合内存模型，不能让事件循环继续工作。主线程仍在等待单个任务，架构上的串行化没有消失。

### Q3：为什么不用条件变量？

条件变量能避免耗 CPU，但仍会阻塞事件线程。Reactor 需要的是异步完成和事件驱动通知，而不是换一种阻塞等待方式。

### Q4：为什么使用 `eventfd`？

它是 Linux 提供的轻量级事件计数描述符，可以注册到 epoll，使工作线程通知、信号管道、监听 socket 和客户端 socket 都由同一个事件循环处理。失败详情仍放在互斥锁保护的队列中，`eventfd` 只负责唤醒。

### Q5：为什么成功路径不也写一次 `eventfd`？

成功处理后，HTTP 状态机会通过 `modfd` 重新武装 `EPOLLONESHOT`，下一次读写就绪自然会回到 epoll。再发一条成功通知只会增加上下文切换和事件处理。失败时需要主线程接管连接及定时器回收，才需要显式通知。

### Q6：`EPOLLONESHOT` 在这里有什么作用？

它保证某个连接事件被取出后，在显式 `modfd` 重新武装之前不会再次被 epoll 报告，从而避免多个线程同时处理同一个连接。工作线程完成当前阶段后，根据状态机把它重新注册为下一次 `EPOLLIN` 或 `EPOLLOUT`。

### Q7：generation 解决什么问题？

它解决失败通知处理阶段的 fd ABA：旧连接关闭后 fd 数字可能被新连接复用，只有 fd 和 generation 都匹配，主线程才执行关闭。它不是智能指针，也不能自动解决所有对象生命周期问题。

### Q8：为什么线程要从 detached 改成 joinable？

detached 线程无法在服务器析构时被可靠等待。如果先释放 `users`、连接池或回调所属对象，后台线程仍可能访问这些资源。joinable 线程允许先发出停止信号、唤醒线程、等待退出，再销毁共享资源。

### Q9：你怎么证明问题来自上游？

一是本项目 README 指向 `qinguoyi/TinyWebServer`；二是官方当前固定提交仍能看到同样的读写自旋和普通标志；三是 `git log -S` 与 `git blame` 将关键代码追溯到官方 2020 年的 Reactor 引入提交；四是我的修复前提交保留相同结构。

### Q10：最初的 1072 倍和 3529 倍后来怎么处理了？

我撤回了这两个性能结论。官方原版和修复前提交重新编译后都无法复现 108/359 的低吞吐，因此旧结果只能作为历史异常，不能用于因果归因。保留错误结果、补做复现、公开修正，比继续挑选有利数字更符合工程规范。

### Q11：官方 README 也有很高的 QPS，为什么你的原版这么低？

补做实验后，官方原版在本机并不低，Reactor 达到约 1.24 万到 1.32 万 req/s。真正异常的是最初那次旧基线，而不是官方原版。当前结论依赖官方固定提交、统一构建、同机同参数和修复前后重编译对照，不再使用无法复现的旧数字。

### Q12：这个项目现在就是生产级了吗？

不是。这次修复解决的是 Reactor 分发和相关生命周期问题。HTTP 解析边界、安全校验、路径访问、超时策略、可观测性、配置管理和更系统的并发测试仍需要继续完善。能主动说明边界比宣称“生产可用”更可信。

## 12. 现场画图版本

修复前：

```text
                    +----------------------+
epoll -> main ----> | append(request)      |
                    | while (!improv) {}   | <--- 主事件循环停在这里
                    +----------+-----------+
                               ^
                               | 普通 int，无同步
                               |
                    +----------+-----------+
                    | worker: read/process|
                    | improv = 1           |
                    +----------------------+
```

修复后：

```text
epoll -> main -> append(work_item) -> 立即继续分发
                |
                v
            worker 执行 IO/业务
                |
                +-- 成功 -> modfd 重新武装 EPOLLONESHOT
                |
                +-- 失败 -> queue{fd,generation} + eventfd
                                                |
                                                v
                                  epoll 唤醒 main 统一回收
```

## 13. 后续如果继续增强，应做什么

1. 对每种模式重复至少 5 次，报告均值、标准差和置信区间。
2. 把自旋移除、`eventfd`、generation、线程退出和 backlog 改动拆成独立提交或实验开关，做消融测试。
3. 使用 `perf stat` 和 `pidstat -t` 记录每线程 CPU、上下文切换和调度行为。
4. 引入支持延迟统计的压测工具，记录平均值、P95、P99，而不只看吞吐。
5. 使用独立压测机，区分客户端与服务器资源竞争。
6. 增加连接中途断开、慢请求、队列满、超时与 fd 高频复用测试。
7. 使用 ThreadSanitizer 验证共享状态，使用 AddressSanitizer 验证定时器和连接生命周期。

## 14. 最终记忆锚点

只记住下面五句话，也能把这个改动讲清楚：

1. **现象**：最初得到 108/359 的异常结果，但官方原版和修复前重编译版都无法复现，因此主动撤回千倍提升结论。
2. **上游证据**：官方从 `7a16b8f9` 引入到本次检查的 `4bcf887`，主事件线程始终在投递后自旋等待普通整型标志。
3. **根因**：事件分发被串行化，忙等争抢 CPU，跨线程普通变量形成数据竞争。
4. **方案**：不可变 `work_item` + 成功路径直接重置 `EPOLLONESHOT` + 失败路径 `eventfd` 回主循环 + generation 过滤迟到关闭。
5. **结果**：修复后 CPU 降低约 34% 到 37%，ET 吞吐基本持平、LT 吞吐本轮下降约 6.4%，两组均 0 失败。
